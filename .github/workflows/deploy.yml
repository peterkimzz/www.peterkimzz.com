name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: yarn

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run ESLint
        run: yarn lint

      - name: Run Prettier check
        run: yarn format:check

      - name: Run TypeScript check
        run: yarn typecheck

      - name: Run unit tests
        run: yarn test:unit

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('yarn.lock') }}

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Build SSR application for E2E
        env:
          NUXT_PUBLIC_SITE_URL: http://127.0.0.1:4173
          STUDIO_GITHUB_CLIENT_ID: test-client
          STUDIO_GITHUB_CLIENT_SECRET: test-secret
          STUDIO_GITHUB_MODERATORS: peterkimzz69@gmail.com
          STUDIO_GITHUB_REDIRECT_URL: http://127.0.0.1:4173/__nuxt_studio/auth/github
        run: yarn build

      - name: Run Playwright E2E
        env:
          CI: true
          NUXT_PUBLIC_SITE_URL: http://127.0.0.1:4173
          STUDIO_GITHUB_CLIENT_ID: test-client
          STUDIO_GITHUB_CLIENT_SECRET: test-secret
          STUDIO_GITHUB_MODERATORS: peterkimzz69@gmail.com
          STUDIO_GITHUB_REDIRECT_URL: http://127.0.0.1:4173/__nuxt_studio/auth/github
        run: yarn test:e2e:ci

      - name: Generate GitHub Pages fallback
        env:
          NUXT_PUBLIC_SITE_URL: https://www.peterkimzz.com
        run: yarn generate

      - name: Verify sitemap exists
        run: test -f .output/public/sitemap.xml

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: .output/public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
